#!/usr/bin/env bash
# Finish a prepared server bootstrap after managed files and hooks are applied.
# Invoked by chezmoi; no-op unless this machine has an unfinished bootstrap.
set -euo pipefail
{{ if eq (.profile | lower) "server" }}
exec bash {{ joinPath .chezmoi.sourceDir "scripts/bootstrap-server.sh" | quote }} finish
{{ end }}
