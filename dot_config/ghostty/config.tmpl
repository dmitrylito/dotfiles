# Ghostty Configuration Template managed by Chezmoi
# Profiles: omarchy (Linux), mac (macOS)
{{ if eq .profile "omarchy" -}}
# --- Omarchy Profile Configuration ---

# Dynamic theme colors from Omarchy
config-file = ?"~/.local/state/omarchy/current/theme/ghostty.conf"

# Font size
font-size = 10

# Window
gtk-toolbar-style = flat

# Keyboard bindings
keybind = shift+insert=paste_from_clipboard
keybind = control+insert=copy_to_clipboard
# Send CSI-u so Herdr can distinguish Alt+Shift+Enter from Alt+Enter.
keybind = alt+shift+enter=csi:13;4u
# Send Shift+Enter as CSI-u so TUIs can distinguish it from Enter.
keybind = shift+enter=csi:13;2u

# Fix general slowness on hyprland (https://github.com/ghostty-org/ghostty/discussions/3224)
async-backend = epoll

{{- else if eq .profile "mac" -}}
# --- macOS Profile Configuration ---

# Theme (Built-in "TokyoNight Night")
theme = "TokyoNight Night"


# Font size
font-size = 13

macos-titlebar-style = hidden

# Option key as Alt (essential for standard command-line navigation/shortcuts)
macos-option-as-alt = true

{{- end }}

# --- Global Configuration ---

# Font
font-family = "JetBrainsMono Nerd Font"
font-style = Regular

app-notifications = no-clipboard-copy

# Answer OSC 52 read queries without prompting, so Neovim's OSC 52 paste
# (used inside Herdr/SSH) doesn't hang waiting for the terminal.
clipboard-read = allow


# Window
confirm-close-surface = false
resize-overlay = never
background-opacity-cells = true

# Cursor styling
cursor-style = "block"
cursor-style-blink = false

# Shell integration
shell-integration-features = no-cursor,ssh-env,no-title

# Keyboard bindings
keybind = super+control+shift+alt+arrow_down=resize_split:down,100
keybind = super+control+shift+alt+arrow_up=resize_split:up,100
keybind = super+control+shift+alt+arrow_left=resize_split:left,100
keybind = super+control+shift+alt+arrow_right=resize_split:right,100

# Mouse
mouse-scroll-multiplier = 1.5

# Window & Padding
window-padding-x = 20
window-padding-y = 20

{{ if eq .profile "mac" -}}
background = 1a1a1a
{{- else -}}
background = 000000
{{- end }}
