# Voxtype Configuration
#
# All settings can be overridden via CLI flags
#
# Values are tuned for this box's GPU and for Wayland's uinput typing path (dotool).
engine = "parakeet"

# State file for external integrations (Waybar, polybar, etc.)
# Use "auto" for default location ($XDG_RUNTIME_DIR/voxtype/state),
# a custom path, or "disabled" to turn off. The daemon writes state
# ("idle", "recording", "transcribing") to this file whenever it changes.
# Required for `voxtype record toggle` and `voxtype status` commands.
state_file = "auto"

[hotkey]
# Hotkey is configured in Hyprland. Default is Super + Ctrl + X
enabled = false
key = "HOME"
mode = "toggle"

[audio]
device = "default"

# Sample rate in Hz (whisper expects 16000)
sample_rate = 16000

max_duration_secs = 300

pause_media = false
duck_media = false
duck_media_volume_percent = 70
duck_media_fade_ms = 150

[audio.feedback]
enabled = false
theme = "default"
volume = 0.2

[output]
mode = "type"
fallback_to_clipboard = true

type_delay_ms = 0
auto_submit = false
shift_enter_newlines = false

driver_order = ["dotool", "clipboard"]

pre_type_delay_ms = 25

[output.notification]
on_recording_start = false
on_recording_stop = false
on_transcription = false
show_engine_icon = false

[text]
spoken_punctuation = false
smart_auto_submit = false

[meeting]
enabled = false

[meeting.diarization]
enabled = false

[meeting.audio]
source = "default"

[vad]
enabled = false
backend = "auto"
threshold = 0.5

[status]
icon_theme = "omarchy"

[whisper]
gpu_isolation = false
on_demand_loading = false
flash_attention = false
eager_processing = true
model = ""
mode = "local"
language = "auto"
translate = false

[parakeet]
model = "parakeet-unified-en-0.6b"
on_demand_loading = false
streaming = true
streaming_chunk_secs = 0.16
streaming_left_context_secs = 8
streaming_right_context_secs = 1.6
